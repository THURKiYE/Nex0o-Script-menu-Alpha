
-- Nex0o Hub - Universal Roblox Exploit GUI
-- Made for Xeno Executor
-- Dark Modern Design with Green Theme

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Mouse = LocalPlayer:GetMouse()
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local Lighting = game:GetService("Lighting")

-- Anti-VC Bypass
if game:GetService("VoiceChatService") then
    game:GetService("VoiceChatService"):SetVoiceEnabled(false)
end

-- GUI Erstellen
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Nex0oHub"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = CoreGui

-- Main Frame
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 400, 0, 500)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -250)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- Glow-Effekt um Mauszeiger
local MouseGlow = Instance.new("Frame")
MouseGlow.Name = "MouseGlow"
MouseGlow.Size = UDim2.new(0, 50, 0, 50)
MouseGlow.BackgroundTransparency = 1
MouseGlow.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(1, 0)
UICorner.Parent = MouseGlow

local UIGradient = Instance.new("UIGradient")
UIGradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 255, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 200, 0))
}
UIGradient.Rotation = 45
UIGradient.Parent = MouseGlow

-- Header
local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Size = UDim2.new(1, 0, 0, 40)
Header.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
Header.BorderSizePixel = 0
Header.Parent = MainFrame

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(0, 200, 1, 0)
Title.BackgroundTransparency = 1
Title.Text = "Nex0o Hub"
Title.TextColor3 = Color3.fromRGB(0, 255, 0)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 18
Title.Parent = Header

-- Color Picker Button
local ColorPicker = Instance.new("TextButton")
ColorPicker.Name = "ColorPicker"
ColorPicker.Size = UDim2.new(0, 80, 0, 25)
ColorPicker.Position = UDim2.new(1, -90, 0.5, -12.5)
ColorPicker.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
ColorPicker.Text = "Farbe ändern"
ColorPicker.TextColor3 = Color3.fromRGB(0, 255, 0)
ColorPicker.Font = Enum.Font.Gotham
ColorPicker.TextSize = 12
ColorPicker.Parent = Header

local ColorPickerCorner = Instance.new("UICorner")
ColorPickerCorner.CornerRadius = UDim.new(0, 4)
ColorPickerCorner.Parent = ColorPicker

local ColorPickerStroke = Instance.new("UIStroke")
ColorPickerStroke.Color = Color3.fromRGB(0, 255, 0)
ColorPickerStroke.Thickness = 1
ColorPickerStroke.Parent = ColorPicker

-- Content Frame
local ContentFrame = Instance.new("ScrollingFrame")
ContentFrame.Name = "ContentFrame"
ContentFrame.Size = UDim2.new(1, -20, 1, -60)
ContentFrame.Position = UDim2.new(0, 10, 0, 50)
ContentFrame.BackgroundTransparency = 1
ContentFrame.BorderSizePixel = 0
ContentFrame.ScrollBarThickness = 5
ContentFrame.ScrollBarImageColor3 = Color3.fromRGB(0, 255, 0)
ContentFrame.Parent = MainFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.Padding = UDim.new(0, 10)
UIListLayout.Parent = ContentFrame

-- Funktionen
local currentColor = Color3.fromRGB(0, 255, 0)

local function createToggle(name, defaultState, callback)
    local ToggleFrame = Instance.new("Frame")
    ToggleFrame.Name = name .. "Toggle"
    ToggleFrame.Size = UDim2.new(1, 0, 0, 40)
    ToggleFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    ToggleFrame.BorderSizePixel = 0
    ToggleFrame.Parent = ContentFrame
    
    local ToggleCorner = Instance.new("UICorner")
    ToggleCorner.CornerRadius = UDim.new(0, 6)
    ToggleCorner.Parent = ToggleFrame
    
    local ToggleStroke = Instance.new("UIStroke")
    ToggleStroke.Color = currentColor
    ToggleStroke.Thickness = 1
    ToggleStroke.Parent = ToggleFrame
    
    local ToggleLabel = Instance.new("TextLabel")
    ToggleLabel.Name = "Label"
    ToggleLabel.Size = UDim2.new(0.7, 0, 1, 0)
    ToggleLabel.BackgroundTransparency = 1
    ToggleLabel.Text = name
    ToggleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    ToggleLabel.Font = Enum.Font.Gotham
    ToggleLabel.TextSize = 14
    ToggleLabel.TextXAlignment = Enum.TextXAlignment.Left
    ToggleLabel.Parent = ToggleFrame
    
    local ToggleButton = Instance.new("TextButton")
    ToggleButton.Name = "ToggleButton"
    ToggleButton.Size = UDim2.new(0, 50, 0, 25)
    ToggleButton.Position = UDim2.new(1, -60, 0.5, -12.5)
    ToggleButton.BackgroundColor3 = defaultState and currentColor or Color3.fromRGB(40, 40, 45)
    ToggleButton.Text = ""
    ToggleButton.Parent = ToggleFrame
    
    local ToggleButtonCorner = Instance.new("UICorner")
    ToggleButtonCorner.CornerRadius = UDim.new(0, 12)
    ToggleButtonCorner.Parent = ToggleButton
    
    local ToggleCircle = Instance.new("Frame")
    ToggleCircle.Name = "ToggleCircle"
    ToggleCircle.Size = UDim2.new(0, 21, 0, 21)
    ToggleCircle.Position = defaultState and UDim2.new(1, -23, 0.5, -10.5) or UDim2.new(0, 2, 0.5, -10.5)
    ToggleCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    ToggleCircle.Parent = ToggleButton
    
    local CircleCorner = Instance.new("UICorner")
    CircleCorner.CornerRadius = UDim.new(1, 0)
    CircleCorner.Parent = ToggleCircle
    
    local state = defaultState
    
    ToggleButton.MouseButton1Click:Connect(function()
        state = not state
        if state then
            ToggleButton.BackgroundColor3 = currentColor
            ToggleCircle:TweenPosition(UDim2.new(1, -23, 0.5, -10.5), "Out", "Quad", 0.2)
        else
            ToggleButton.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
            ToggleCircle:TweenPosition(UDim2.new(0, 2, 0.5, -10.5), "Out", "Quad", 0.2)
        end
        callback(state)
    end)
    
    return ToggleFrame
end

-- Maus Glow Update
RunService.RenderStepped:Connect(function()
    MouseGlow.Position = UDim2.new(0, Mouse.X - 25, 0, Mouse.Y - 25)
end)

-- Funktion zum Ändern der Design-Farbe
local function updateColor(newColor)
    currentColor = newColor
    
    -- Update all UI elements with new color
    for _, element in pairs(ScreenGui:GetDescendants()) do
        if element:IsA("UIStroke") then
            element.Color = newColor
        elseif element.Name == "ToggleButton" and element:IsA("TextButton") then
            local circle = element:FindFirstChild("ToggleCircle")
            if circle then
                local circlePos = circle.Position
                if circlePos == UDim2.new(1, -23, 0.5, -10.5) then
                    element.BackgroundColor3 = newColor
                end
            end
        end
    end
    
    -- Update Mouse Glow
    MouseGlow.UIGradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, newColor),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(newColor.R * 200/255, newColor.G * 200/255, newColor.B * 200/255))
    }
    
    Title.TextColor3 = newColor
    ColorPicker.TextColor3 = newColor
    ContentFrame.ScrollBarImageColor3 = newColor
end

-- Color Picker Funktion
ColorPicker.MouseButton1Click:Connect(function()
    local colorPickerGui = Instance.new("ScreenGui", CoreGui)
    local colorFrame = Instance.new("Frame", colorPickerGui)
    colorFrame.Size = UDim2.new(0, 200, 0, 150)
    colorFrame.Position = UDim2.new(0.5, -100, 0.5, -75)
    colorFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
    
    local colors = {
        Color3.fromRGB(0, 255, 0),    -- Grün (Default)
        Color3.fromRGB(0, 150, 255),  -- Blau
        Color3.fromRGB(255, 50, 50),  -- Rot
        Color3.fromRGB(255, 255, 0),  -- Gelb
        Color3.fromRGB(255, 0, 255)   -- Pink
    }
    
    for i, color in ipairs(colors) do
        local colorButton = Instance.new("TextButton", colorFrame)
        colorButton.Size = UDim2.new(0.8, 0, 0, 30)
        colorButton.Position = UDim2.new(0.1, 0, (i-1)*0.2 + 0.1, 0)
        colorButton.BackgroundColor3 = color
        colorButton.Text = ""
        colorButton.MouseButton1Click:Connect(function()
            updateColor(color)
            colorPickerGui:Destroy()
        end)
    end
end)

-- Exploit Funktionen
local FlyEnabled = false
local GodModeEnabled = false
local InvisibleEnabled = false
local ESPEnabled = false

-- Alle Emotes Funktion
local function AllEmotes()
    local emotes = {
        "wave", "point", "cheer", "laugh", "dance", "dance2", "dance3",
        "floss", "kill", "angry", "sit", "lay", "jump", "clap"
    }
    
    for _, emote in pairs(emotes) do
        game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest"):FireServer("/e " .. emote, "All")
    end
end

-- Jerk Emote
local function JerkEmote()
    game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest"):FireServer("/e jerk", "All")
end

-- Bang Emote
local function BangEmote()
    game:GetService("ReplicatedStorage"):WaitForChild("DefaultChatSystemChatEvents"):WaitForChild("SayMessageRequest"):FireServer("/e bang", "All")
end

-- Fliegen wie HD Admin
local function Fly()
    FlyEnabled = not FlyEnabled
    
    if FlyEnabled then
        local BodyGyro = Instance.new("BodyGyro")
        BodyGyro.P = 9e4
        BodyGyro.maxTorque = Vector3.new(9e9, 9e9, 9e9)
        BodyGyro.cframe = LocalPlayer.Character.HumanoidRootPart.CFrame
        BodyGyro.Parent = LocalPlayer.Character.HumanoidRootPart
        
        local BodyVelocity = Instance.new("BodyVelocity")
        BodyVelocity.velocity = Vector3.new(0, 0, 0)
        BodyVelocity.maxForce = Vector3.new(9e9, 9e9, 9e9)
        BodyVelocity.Parent = LocalPlayer.Character.HumanoidRootPart
        
        local flying = true
        
        LocalPlayer.Character.Humanoid.PlatformStand = true
        
        spawn(function()
            repeat wait()
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    BodyGyro.cframe = workspace.CurrentCamera.CoordinateFrame
                    BodyVelocity.velocity = Vector3.new(0, 0, 0)
                    
                    if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.W) then
                        BodyVelocity.velocity = (workspace.CurrentCamera.CoordinateFrame.lookVector * 50) + BodyVelocity.velocity
                    end
                    if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.S) then
                        BodyVelocity.velocity = (workspace.CurrentCamera.CoordinateFrame.lookVector * -50) + BodyVelocity.velocity
                    end
                    if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.A) then
                        BodyVelocity.velocity = (workspace.CurrentCamera.CoordinateFrame.rightVector * -50) + BodyVelocity.velocity
                    end
                    if game:GetService("UserInputService"):IsKeyDown(Enum.KeyCode.D) then
                        BodyVelocity.velocity = (workspace.CurrentCamera.CoordinateFrame.rightVector * 50) + BodyVelocity.velocity
                    end
                end
            until not flying
        end)
        
        LocalPlayer.CharacterAdded:Connect(function()
            flying = false
            FlyEnabled = false
        end)
    else
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.Humanoid.PlatformStand = false
            for _, v in pairs(LocalPlayer.Character.HumanoidRootPart:GetChildren()) do
                if v:IsA("BodyGyro") or v:IsA("BodyVelocity") then
                    v:Destroy()
                end
            end
        end
    end
end

-- Godmode (Health Lock)
local function GodMode(state)
    GodModeEnabled = state
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        if state then
            LocalPlayer.Character.Humanoid.MaxHealth = math.huge
            LocalPlayer.Character.Humanoid.Health = math.huge
        else
            LocalPlayer.Character.Humanoid.MaxHealth = 100
            LocalPlayer.Character.Humanoid.Health = 100
        end
    end
end

-- Unsichtbar machen
local function Invisible(state)
    InvisibleEnabled = state
    if LocalPlayer.Character then
        for _, part in pairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                part.Transparency = state and 1 or 0
            end
        end
    end
end

-- ESP Funktion
local function ESP(state)
    ESPEnabled = state
    
    if state then
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local highlight = Instance.new("Highlight")
                highlight.Name = player.Name .. "_ESP"
                highlight.FillColor = Color3.fromRGB(255, 0, 0)
                highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                highlight.FillTransparency = 0.5
                highlight.OutlineTransparency = 0
                highlight.Parent = player.Character
            end
        end
        
        Players.PlayerAdded:Connect(function(player)
            player.CharacterAdded:Connect(function(char)
                if ESPEnabled then
                    wait(1)
                    local highlight = Instance.new("Highlight")
                    highlight.Name = player.Name .. "_ESP"
                    highlight.FillColor = Color3.fromRGB(255, 0, 0)
                    highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    highlight.FillTransparency = 0.5
                    highlight.OutlineTransparency = 0
                    highlight.Parent = char
                end
            end)
        end)
    else
        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local esp = player.Character:FindFirstChild(player.Name .. "_ESP")
                if esp then
                    esp:Destroy()
                end
            end
        end
    end
end

-- Toggles erstellen
createToggle("Alle Emotes", false, function(state)
    if state then AllEmotes() end
end)

createToggle("Fliegen", false, function(state)
    Fly()
end)

createToggle("Godmode", false, function(state)
    GodMode(state)
end)

createToggle("Unsichtbar", false, function(state)
    Invisible(state)
end)

createToggle("Player ESP", false, function(state)
    ESP(state)
end)

-- Emote Buttons erstellen (separate Buttons für Jerk und Bang)
local function createEmoteButton(name, callback)
    local ButtonFrame = Instance.new("Frame")
    ButtonFrame.Name = name .. "Button"
    ButtonFrame.Size = UDim2.new(1, 0, 0, 35)
    ButtonFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    ButtonFrame.BorderSizePixel = 0
    ButtonFrame.Parent = ContentFrame
    
    local ButtonCorner = Instance.new("UICorner")
    ButtonCorner.CornerRadius = UDim.new(0, 6)
    ButtonCorner.Parent = ButtonFrame
    
    local ButtonStroke = Instance.new("UIStroke")
    ButtonStroke.Color = currentColor
    ButtonStroke.Thickness = 1
    ButtonStroke.Parent = ButtonFrame
    
    local ButtonLabel = Instance.new("TextButton")
    ButtonLabel.Name = "Button"
    ButtonLabel.Size = UDim2.new(1, -20, 1, -10)
    ButtonLabel.Position = UDim2.new(0, 10, 0, 5)
    ButtonLabel.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
    ButtonLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    ButtonLabel.Text = name .. " Emote"
    ButtonLabel.Font = Enum.Font.GothamBold
    ButtonLabel.TextSize = 14
    ButtonLabel.Parent = ButtonFrame
    
    local LabelCorner = Instance.new("UICorner")
    LabelCorner.CornerRadius = UDim.new(0, 4)
    LabelCorner.Parent = ButtonLabel
    
    ButtonLabel.MouseButton1Click:Connect(callback)
    
    return ButtonFrame
end

-- Emote Buttons hinzufügen
createEmoteButton("Jerk", JerkEmote)
createEmoteButton("Bang", BangEmote)

-- Hotkey für alle Emotes (. Taste)
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.KeyCode == Enum.KeyCode.Period then
        AllEmotes()
    end
end)

-- Character Added Events für persistente Features
LocalPlayer.CharacterAdded:Connect(function()
    if GodModeEnabled then
        wait(1)
        GodMode(true)
    end
    if InvisibleEnabled then
        wait(1)
        Invisible(true)
    end
end)

-- Initialisiere Features wenn schon ein Character existiert
if LocalPlayer.Character then
    spawn(function()
        wait(2)
        if GodModeEnabled then GodMode(true) end
        if InvisibleEnabled then Invisible(true) end
    end)
end

-- GUI schließen mit Rechtsklick
game:GetService("UserInputService").InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton2 then
        MainFrame.Visible = not MainFrame.Visible
        MouseGlow.Visible = MainFrame.Visible
    end
end)

print("Nex0o Hub erfolgreich geladen! Rechtsklick zum Verstecken/Anzeigen.")
